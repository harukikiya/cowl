#!/bin/sh
# 1行受け取ったら「JSON でない行」を返すダミーサーバ。
# 行プロトコルの枠組みが信用できない応答でクライアントが fail する経路
# （P1-1 の修正）を固定するための治具。応答後は生き続ける（exit との
# レースを作らず、fail がクライアント側の判断であることを確実にするため）
read -r line
echo "not json"
exec sleep 1000
